# Set PYTHONPATH to path to the working directory + /code
# This is for the "bash" shell on Unix / git bash on Windows
# Add this to your python path before running it
export PYTHONPATH="${PYTHONPATH}:${PWD}:/home/centos/ceph-prefetching/Kariz/code
"
